export interface DataType {
  id: number;
  year: number | string;
  content: string;
}

export interface ProjectDataType {
  id: number;
  name: string;
  language: string[];
  Library: string;
  link: string;
}
